import { VehicleShowDirective } from './vehicle-show.directive';

describe('VehicleShowDirective', () => {
  it('should create an instance', () => {
    const directive = new VehicleShowDirective();
    expect(directive).toBeTruthy();
  });
});
