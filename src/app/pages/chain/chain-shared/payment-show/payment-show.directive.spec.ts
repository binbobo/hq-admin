import { PaymentShowDirective } from './payment-show.directive';

describe('PaymentShowDirective', () => {
  it('should create an instance', () => {
    const directive = new PaymentShowDirective();
    expect(directive).toBeTruthy();
  });
});
