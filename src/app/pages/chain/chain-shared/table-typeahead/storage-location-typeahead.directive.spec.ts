import { StorageLocationTypeaheadDirective } from './storage-location-typeahead.directive';

describe('StorageLocationTypeaheadDirective', () => {
  it('should create an instance', () => {
    const directive = new StorageLocationTypeaheadDirective();
    expect(directive).toBeTruthy();
  });
});
