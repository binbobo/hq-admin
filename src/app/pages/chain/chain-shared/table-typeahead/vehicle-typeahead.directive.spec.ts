import { VehicleTypeaheadDirective } from './vehicle-typeahead.directive';

describe('VehicleTypeaheadDirective', () => {
  it('should create an instance', () => {
    const directive = new VehicleTypeaheadDirective();
    expect(directive).toBeTruthy();
  });
});
