import { ProductTypeaheadDirective } from './product-typeahead.directive';

describe('ProductTypeaheadDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductTypeaheadDirective();
    expect(directive).toBeTruthy();
  });
});
