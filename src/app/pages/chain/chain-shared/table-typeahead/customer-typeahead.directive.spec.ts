import { CustomerTypeaheadDirective } from './customer-typeahead.directive';

describe('CustomerTypeaheadDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomerTypeaheadDirective();
    expect(directive).toBeTruthy();
  });
});
