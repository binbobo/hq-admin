import { ProviderTypeaheadDirective } from './provider-typeahead.directive';

describe('ProviderTypeaheadDirective', () => {
  it('should create an instance', () => {
    const directive = new ProviderTypeaheadDirective();
    expect(directive).toBeTruthy();
  });
});
