import { ProductCategoryTypeaheadDirective } from './product-category-typeahead.directive';

describe('ProductCategoryTypeaheadDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductCategoryTypeaheadDirective();
    expect(directive).toBeTruthy();
  });
});
