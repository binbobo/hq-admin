import { ProductBrandTypeaheadDirective } from './product-brand-typeahead.directive';

describe('ProductBrandTypeaheadDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductBrandTypeaheadDirective();
    expect(directive).toBeTruthy();
  });
});
