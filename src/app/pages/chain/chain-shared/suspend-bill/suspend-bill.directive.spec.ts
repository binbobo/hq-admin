import { SuspendBillDirective } from './suspend-bill.directive';

describe('SuspendBillDirective', () => {
  it('should create an instance', () => {
    const directive = new SuspendBillDirective();
    expect(directive).toBeTruthy();
  });
});
