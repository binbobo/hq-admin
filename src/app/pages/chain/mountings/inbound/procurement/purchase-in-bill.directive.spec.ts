import { PurchaseInBillDirective } from './purchase-in-bill.directive';

describe('PurchaseInBillDirective', () => {
  it('should create an instance', () => {
    const directive = new PurchaseInBillDirective();
    expect(directive).toBeTruthy();
  });
});
