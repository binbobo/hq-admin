import { ReceiveOutBillDirective } from './receive-out-bill.directive';

describe('ReceiveOutBillDirective', () => {
  it('should create an instance', () => {
    const directive = new ReceiveOutBillDirective();
    expect(directive).toBeTruthy();
  });
});
