import { PurchaseOutBillDirective } from './purchase-out-bill.directive';

describe('PurchaseOutBillDirective', () => {
  it('should create an instance', () => {
    const directive = new PurchaseOutBillDirective();
    expect(directive).toBeTruthy();
  });
});
