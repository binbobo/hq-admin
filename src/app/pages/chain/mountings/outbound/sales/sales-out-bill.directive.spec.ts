import { SalesOutBillDirective } from './sales-out-bill.directive';

describe('SalesOutBillDirective', () => {
  it('should create an instance', () => {
    const directive = new SalesOutBillDirective();
    expect(directive).toBeTruthy();
  });
});
