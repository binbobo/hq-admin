import { HqModalDirective } from './hq-modal.directive';

describe('HqModalDirective', () => {
  it('should create an instance', () => {
    const directive = new HqModalDirective();
    expect(directive).toBeTruthy();
  });
});
