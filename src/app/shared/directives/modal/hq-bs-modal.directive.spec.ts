import { HqBsModalDirective } from './hq-bs-modal.directive';

describe('HqBsModalDirective', () => {
  it('should create an instance', () => {
    const directive = new HqBsModalDirective();
    expect(directive).toBeTruthy();
  });
});
