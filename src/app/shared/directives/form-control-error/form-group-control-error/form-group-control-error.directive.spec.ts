import { FormGroupControlErrorDirective } from './form-group-control-error.directive';

describe('FormGroupControlErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new FormGroupControlErrorDirective();
    expect(directive).toBeTruthy();
  });
});
