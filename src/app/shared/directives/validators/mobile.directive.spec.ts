import { MobileValidator } from './mobile.directive';

describe('MobileDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileValidator();
    expect(directive).toBeTruthy();
  });
});
