import { TelValidator } from './tel.directive';

describe('TelDirective', () => {
  it('should create an instance', () => {
    const directive = new TelValidator();
    expect(directive).toBeTruthy();
  });
});
