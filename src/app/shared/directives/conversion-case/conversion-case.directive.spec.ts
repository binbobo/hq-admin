import { ConversionCaseDirective } from './conversion-case.directive';

describe('ConversionCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new ConversionCaseDirective();
    expect(directive).toBeTruthy();
  });
});
