import { TableTypeaheadDirective } from './table-typeahead.directive';

describe('TableTypeaheadDirective', () => {
  it('should create an instance', () => {
    const directive = new TableTypeaheadDirective();
    expect(directive).toBeTruthy();
  });
});
