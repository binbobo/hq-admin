import { NgModelTrimDirective } from './ng-model-trim.directive';

describe('NgModelTrimDirective', () => {
  it('should create an instance', () => {
    const directive = new NgModelTrimDirective();
    expect(directive).toBeTruthy();
  });
});
