import { FormControlNameTrimDirective } from './form-control-name-trim.directive';

describe('FormControlNameTrimDirective', () => {
  it('should create an instance', () => {
    const directive = new FormControlNameTrimDirective();
    expect(directive).toBeTruthy();
  });
});
