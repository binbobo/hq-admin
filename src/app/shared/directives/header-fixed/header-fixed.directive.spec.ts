import { HeaderFixedDirective } from './header-fixed.directive';

describe('HeaderFixedDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderFixedDirective();
    expect(directive).toBeTruthy();
  });
});
