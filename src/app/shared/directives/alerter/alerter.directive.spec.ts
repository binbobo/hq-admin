import { HqAlerter }  from './hq-alerter.directive';

describe('hq-alerterDirective', () => {
  it('should create an instance', () => {
    const directive = new hq-alerter();
    expect(directive).toBeTruthy();
  });
});
