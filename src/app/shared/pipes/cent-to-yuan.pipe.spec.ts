import { CentToYuanPipe } from './cent-to-yuan.pipe';

describe('CentToYuanPipe', () => {
  it('create an instance', () => {
    const pipe = new CentToYuanPipe();
    expect(pipe).toBeTruthy();
  });
});
