import { DurationHumanizePipe } from './duration-humanize.pipe';

describe('DurationHumanizePipe', () => {
  it('create an instance', () => {
    const pipe = new DurationHumanizePipe();
    expect(pipe).toBeTruthy();
  });
});
