import { StandardDatetimePipe } from './standard-datetime.pipe';

describe('StandardDatetimePipe', () => {
  it('create an instance', () => {
    const pipe = new StandardDatetimePipe();
    expect(pipe).toBeTruthy();
  });
});
